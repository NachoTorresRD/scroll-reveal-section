"use strict";(()=>{const elements=[...document.querySelectorAll(".reveal")],replay=document.querySelector("#replay"),replayBottom=document.querySelector("#replay-bottom"),progress=document.querySelector("#progress"),status=document.querySelector("#status"),reduced=window.matchMedia("(prefers-reduced-motion: reduce)");if(!replay||!replayBottom||!progress||!status){console.warn("Scroll Reveal: DOM incompleto.");return}let observer=null,frame=0;const observe=()=>{observer?.disconnect();if(reduced.matches){elements.forEach(element=>element.classList.add("is-visible"));return}observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.18,rootMargin:"0px 0px -8%"});elements.forEach(element=>observer.observe(element))};const updateProgress=()=>{const max=document.documentElement.scrollHeight-innerHeight,ratio=max>0?scrollY/max:0;progress.style.transform=`scaleX(${ratio})`;frame=0};addEventListener("scroll",()=>{if(!frame)frame=requestAnimationFrame(updateProgress)},{passive:true});const restart=()=>{elements.forEach(element=>element.classList.remove("is-visible"));scrollTo({top:0,behavior:reduced.matches?"auto":"smooth"});setTimeout(observe,reduced.matches?0:450);status.textContent="Animaciones reiniciadas"};replay.addEventListener("click",restart);replayBottom.addEventListener("click",restart);reduced.addEventListener("change",observe);observe();updateProgress()})();
